[build]
builder = "DOCKERFILE"
dockerfilePath = "../../Dockerfile.dashboard"
buildCommand = "npm run db:generate && npm run db:migrate && npm run build"

[deploy]
startCommand = "node apps/dashboard/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "dashboard"
